package com.one.memorymatch.ui.theme

import androidx.compose.ui.graphics.Color

// Kid Palette - Core
val SkyTop = Color(0xFFB3E5FC)
val SkyBottom = Color(0xFFE1F5FE)
val BackgroundLight = Color(0xFFF8FBFE)
val SurfaceWhite = Color(0xFFFFFFFF)
val TextDark = Color(0xFF263238)
val TextLight = Color(0xFFFFFFFF)
val TextMuted = Color(0xFF78909C)

// Stars
val StarGold = Color(0xFFFFD700)
val StarGoldDark = Color(0xFFFFA000)
val StarEmpty = Color(0xFFE0E0E0)
val StarEmptyDark = Color(0xFFBDBDBD)

// Actions
val ButtonPrimary = Color(0xFFFF7043)
val ButtonPrimaryDark = Color(0xFFE64A19)
val ButtonSecondary = Color(0xFF42A5F5)
val ButtonSecondaryDark = Color(0xFF1976D2)
val ButtonSuccess = Color(0xFF66BB6A)
val ButtonSuccessDark = Color(0xFF388E3C)
val ButtonWarning = Color(0xFFFFA000)
val WarningRed = Color(0xFFE53935)

// 8 Pack Colors + All Mix
object PackColors {
    // Zoo (Green)
    val ZooPrimary = Color(0xFF4CAF50)
    val ZooDark = Color(0xFF2E7D32)

    // Farm (Amber)
    val FarmPrimary = Color(0xFFFFA000)
    val FarmDark = Color(0xFFE65100)

    // Sea (Blue)
    val SeaPrimary = Color(0xFF2196F3)
    val SeaDark = Color(0xFF0D47A1)

    // Birds (Purple)
    val BirdsPrimary = Color(0xFF9C27B0)
    val BirdsDark = Color(0xFF6A1B9A)

    // Fruits (Pink/Red)
    val FruitsPrimary = Color(0xFFE91E63)
    val FruitsDark = Color(0xFFC2185B)

    // Vegetables (Light Green)
    val VegetablesPrimary = Color(0xFF8BC34A)
    val VegetablesDark = Color(0xFF558B2F)

    // Vehicles (Orange)
    val VehiclesPrimary = Color(0xFFFF9800)
    val VehiclesDark = Color(0xFFE65100)

    // Shapes & Colors (Teal)
    val ShapesColorsPrimary = Color(0xFF009688)
    val ShapesColorsDark = Color(0xFF00695C)

    // All Mix (Vibrant Violet / Rainbow)
    val AllMixPrimary = Color(0xFF7C4DFF)
    val AllMixDark = Color(0xFF512DA8)
}
